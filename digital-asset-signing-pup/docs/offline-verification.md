# Offline Verification

With cached metadata and proofs, offline mode can verify:
- manifest signatures
- DSSE/in-toto provenance bundles
- artifact digests
- entitlement cache state

Live anchoring and network fetch operations remain unavailable offline.
