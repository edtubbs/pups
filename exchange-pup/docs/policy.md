# safety policy model

Policy checks include:
- kill switch
- destination whitelist (bucket/address)
- confidence threshold
- expected edge threshold
- liquidity threshold
- slippage ceiling
- cooldown
- max single amount
- max daily amount
- recommend-only and paper-only master switches
- explicit live mode flags
- optional human approval requirement

The policy engine is the final authority before execution calls.
